@model IEnumerable<TestTcc2.Models.Musica>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutOuvinte.cshtml";

    //var db = Database.Open("Entities");
    //var selectQuery = "select * from musica";
    var grid = new WebGrid(Model);

}
<h2>Index</h2>

<div>
@grid.GetHtml(
tableStyle:"table table-hover table-bordered",
headerStyle: null,
alternatingRowStyle: "altRow",
columns: grid.Columns(
    grid.Column("Nome","Nome"),
    grid.Column("NomeArtista","Nome do Artista"),       
    grid.Column("Genero","Genero"),
    grid.Column("Metodos",format: (item)=> @Html.ActionLink("Download", "Download", new { path = item.path })) 
    )
   ) 
</div>
