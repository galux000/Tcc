@model TestTcc2.Models.RegisterExternalLoginModel
@{
    ViewBag.Title = "Register";
}

<hgroup class="title">
    <h1>@ViewBag.Title.</h1>
    <h2>Associe sua @ViewBag.ProviderDisplayName conta.</h2>
</hgroup>

@using (Html.BeginForm("ExternalLoginConfirmation", "Account", new { ReturnUrl = ViewBag.ReturnUrl })) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Associe-se</legend>
        <p>
            Você obteve sucesso ao autentificar com <strong>@ViewBag.ProviderDisplayName</strong>.
            Por favor coloque um nome e escolhe um tipo de usuário para usar no site.
        </p>
        <ol>
            <li class="name">
                @Html.LabelFor(m => m.UserName)
                @Html.TextBoxFor(m => m.UserName)
                @Html.ValidationMessageFor(m => m.UserName)
            </li>
            <li>
                @Html.LabelFor(m => m.FullName)
                @Html.TextBoxFor(m => m.FullName)
            </li>
            <li>
                @Html.LabelFor(m =>m.Link)
                @Html.TextBoxFor(m=>m.Link)
            </li>
            <li>
                @Html.RadioButtonFor(m => m.UserType, "Ouvinte", new { id = "Ouvinte" })
                @Html.Label("Ouvinte")
                @Html.RadioButtonFor(m=>m.UserType, "Musico", new { id= "Musico" })
                @Html.Label("Musico")
            </li>
        </ol>
        @Html.HiddenFor(m => m.ExternalLoginData)
        <input type="submit" value="Registrar" />
    </fieldset>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
